services:
  - type: web
    name: docusense
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python aap.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
